mixin attrTag(tag, attrs)
  if typeof attrs === 'object'
      #{tag}/&attributes(attrs)
  else
      #{tag}(val=void 0)/

mixin attrTags(style)
  - var attrTags = {bold: 'b', italic: 'i', strike: 'strike', underline: 'u'}
  each tag, attr in attrTags
    if style[attr]
      +attrTag(tag, style[attr])